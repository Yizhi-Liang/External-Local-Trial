# Codex Project Instructions: External Local RCT HEM

This is a health-economic modeling project. Follow the global and Health-Econ
Codex instructions plus the nearest `CLAUDE.md`.

- Codex implementation requires a concrete handoff plan and may touch only listed
  production files. Review remains read-only; verification writes only approved
  reports, harnesses, or disposable copies.
- Never read the blocked raw digitized survival-data directory or any equivalent
  raw-data path. Use only the clean or model-ready inputs identified by the project.
- Apply HEM construction checks, model parity checks when applicable, and the
  clean-room QC engine only through `~/Claude/hem-qc-engine/adapter/run_qc.sh`.
- Verify source-to-parameter traceability, model behavior, uncertainty analyses,
  generated outputs, and manuscript-number consistency against the frozen plan.
- Use a fresh Codex context for post-implementation review. Do not commit, push,
  publish, or submit without explicit authorization.
- Write all execution artifacts in United States English.
